name: CI

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo registry and target
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: |
            src-tauri -> target

      - name: Install frontend dependencies
        run: npm ci

      - name: TypeScript + frontend build
        run: npm run build

      - name: Rust check (Tauri backend)
        run: cargo check --manifest-path src-tauri/Cargo.toml

      - name: Rust tests (Tauri backend smoke tests)
        run: cargo test --lib --manifest-path src-tauri/Cargo.toml -- --nocapture --test-threads=1
